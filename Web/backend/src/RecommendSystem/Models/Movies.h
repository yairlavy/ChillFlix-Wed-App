#ifndef MOVIES_H
#define MOVIES_H

#include <iostream>
#include <fstream>
#include <string>
#include <set>
using namespace std;

class Movies {
public:
    bool isMovieValid(long int movieID); // Function to check if movie valid
};

#endif // MOVIES_H
